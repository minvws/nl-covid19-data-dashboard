{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "regional_deceased_cbs",
  "type": "object",
  "properties": {
    "values": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/value"
      }
    },
    "last_value": {
      "$ref": "#/definitions/value"
    }
  },
  "required": ["values", "last_value"],
  "additionalProperties": false,
  "definitions": {
    "value": {
      "title": "regional_deceased_cbs_value",
      "type": "object",
      "properties": {
        "registered": {
          "type": "integer"
        },
        "expected": {
          "type": "integer"
        },
        "expected_min": {
          "type": "integer"
        },
        "expected_max": {
          "type": "integer"
        },
        "date_of_report_unix": {
          "type": "integer"
        },
        "date_of_insertion_unix": {
          "type": "integer"
        },
        "vrcode": {
          "type": "string",
          "equalsRootProperty": "code"
        }
      },
      "required": [
        "registered",
        "expected",
        "expected_min",
        "expected_max",
        "date_of_report_unix",
        "date_of_insertion_unix",
        "vrcode"
      ],
      "additionalProperties": false
    }
  }
}
