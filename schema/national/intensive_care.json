{
  "definitions": {
    "item": {
      "title": "intensive_care_value",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "intake_moving_average",
        "beds_occupied",
        "beds_occupied_percentage",
        "beds_capacity",
        "date_of_report_unix",
        "date_of_insertion_unix"
      ],
      "properties": {
        "intake_moving_average": {
          "type": "number"
        },
        "beds_occupied": {
          "type": "number"
        },
        "beds_occupied_percentage": {
          "type": "number"
        },
        "beds_capacity": {
          "type": "number"
        },
        "date_of_report_unix": {
          "type": "integer"
        },
        "date_of_insertion_unix": {
          "type": "integer"
        }
      }
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "intensive_care.json",
  "title": "intensive_care",
  "type": "object",
  "required": ["values", "last_value"],
  "additionalProperties": false,
  "properties": {
    "values": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/item"
      }
    },
    "last_value": {
      "$ref": "#/definitions/item"
    }
  }
}
