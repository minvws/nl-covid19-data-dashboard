{
  "name": "@corona-dashboard/e2e",
  "private": true,
  "version": "0.0.0",
  "license": "EUROPEAN UNION PUBLIC LICENCE v. 1.2",
  "devDependencies": {
    "@types/node": "^16.11.11",
    "@typescript-eslint/eslint-plugin": "^5.5.0",
    "@typescript-eslint/parser": "^5.5.0",
    "cypress": "^8.7.0",
    "eslint": "^8.3.0",
    "eslint-plugin-react": "^7.27.1",
    "typescript": "^4.4.4"
  },
  "scripts": {
    "bootstrap": "echo 'No bootstrap command available for @corona-dashboard/e2e'",
    "clean": "echo 'No clean command available for @corona-dashboard/e2e'",
    "nuke": "yarn clean && npx rimraf node_modules",
    "lint": "eslint 'src/**/*.{js,ts,tsx}'",
    "typecheck": "tsc --pretty --noEmit",
    "test:ci": "echo 'No unit tests available for @corona-dashboard/e2e'",
    "e2e": "cypress open",
    "e2e:ci": "cypress run --headless --browser chrome --config-file ./cypress-ci.json",
    "format": "prettier --write \"src/**/*.{js,ts,tsx}\" \"*.{js,json,md,yml}\"",
    "type-check": "tsc --pretty --noEmit"
  }
}
