{
  "name": "@corona-dashboard/cypress",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.20",
    "cypress": "^6.2.1"
  },
  "scripts": {
    "lint": "echo 'No linter configured for @corona-dashboard/cypress'",
    "compile": "tsc --pretty --noEmit",
    "test:ci": "echo 'No unit tests available for @corona-dashboard/cypress'",
    "cy:open": "cypress open",
    "cy:ci": "cypress run --headless --browser chrome --config-file ./cypress-ci.json"
  }
}
