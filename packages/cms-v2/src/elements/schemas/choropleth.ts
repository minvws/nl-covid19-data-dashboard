import { commonFields, commonPreview } from './shared';

export const choropleth = {
  name: 'choropleth',
  type: 'document',
  title: 'Choropleth',
  fields: [...commonFields],
  preview: commonPreview,
};
